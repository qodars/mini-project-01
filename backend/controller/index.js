const authController = require("./auth");
const categoryControll = require("./category");
const productController = require("./product")

module.exports = {
    authController,
    categoryControll,
    productController
}